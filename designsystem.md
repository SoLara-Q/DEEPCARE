# DeepCare Design System

## 产品气质

DeepCare 面向家庭健康管理，界面应保持可信、温和、清晰和克制。视觉重点是健康数据的可读性与操作安全感，不使用夸张的医疗警示或装饰性动效。

## 颜色

- 主色：`#2563EB`，用于主要操作、链接和选中状态。
- 成功色：`#16A34A`，用于健康状态和完成反馈。
- 警示色：`#D97706`，用于需要关注但不代表紧急的提示。
- 危险色：`#DC2626`，仅用于错误、删除和高风险信息。
- 页面背景：`#F8FAFC`；卡片背景：`#FFFFFF`；主文字：`#0F172A`；辅助文字：`#64748B`。

## 排版与布局

- 默认使用清晰的无衬线字体，正文保持舒适的行高。
- 使用 4px 基础间距单位，常用间距为 8、12、16、24、32px。
- 页面优先采用响应式单列布局，小屏幕不隐藏关键健康数据和操作。
- 数据卡片、表格和表单保持统一圆角、边框和阴影层级，避免高密度装饰。

## 交互与可访问性

- 主要按钮每个区域只保留一个明确的主操作。
- 所有表单控件必须有可见标签、错误提示和键盘焦点状态。
- 删除、停用和处方相关操作需要二次确认，并清楚说明影响。
- 图表、颜色状态和图标不能作为唯一信息表达方式，应同时提供文字或数值。
- 健康建议需标注信息性质，不能将 AI 输出表现为确定性医疗诊断。

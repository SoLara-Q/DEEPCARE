spring:
  datasource:
    url: jdbc:h2:mem:deepcare_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never
  ai:
    chat:
      client:
        enabled: false

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
